actor 输出归一化动作 norm_action[:,1] ∈ [-1,1]
  ↓ 乘以 0.4
物理前轮转角 delta_phy = norm_action[:,1] * 0.4  ∈ [-0.4, 0.4] rad
  ↓ select_action 内 np.interp 同样映射
phy_action[1] = delta_phy

→ 传入 vehicle_manager.apply_control()
  ↓ 再次映射到 CARLA steer
steer_val = np.interp(delta_phy, [-0.4, 0.4], [-0.67, 0.67])
  ↓ 直接赋值给 VehicleControl.steer
CARLA 正 steer → 车轮右转
